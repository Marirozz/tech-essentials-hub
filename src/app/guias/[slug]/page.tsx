import Image from "next/image";import { notFound } from "next/navigation";import { guides,products } from "@/data/content";import { ProductCard } from "@/components/ProductCard";
export function generateStaticParams(){return guides.map(g=>({slug:g.slug}))}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(x=>x.slug===slug);if(!g)notFound();return <main><section className="section"><div className="container article"><span className="badge">{g.category}</span><h1>{g.title}</h1><p className="muted">Tiempo de lectura: {g.minutes} minutos</p><Image src={g.image} alt={g.title} width={1200} height={720} style={{borderRadius:24,margin:"28px 0"}}/><p>{g.excerpt}</p><h2>Qué debes tener en cuenta</h2><p>Define primero tu necesidad, presupuesto y espacio disponible. No siempre el producto con más funciones es el más conveniente.</p><h2>Nuestra selección</h2><p>Estos productos representan distintos presupuestos y necesidades. Revisa la ficha de cada uno antes de comprar.</p></div></section><section className="section"><div className="container grid grid-3">{products.slice(0,3).map(p=><ProductCard key={p.slug} product={p}/>)}</div></section></main>}
