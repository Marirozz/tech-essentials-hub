import { ProductCard } from "@/components/ProductCard";import { products } from "@/data/content";
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=slug.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ");const filtered=products.filter(p=>p.category.toLowerCase().replaceAll(" ","-")===slug);return <main className="section"><div className="container"><span className="eyebrow">Categoría</span><h1>{title}</h1><p className="subtitle">Productos y guías seleccionados para esta categoría.</p><div className="grid grid-3" style={{marginTop:36}}>{(filtered.length?filtered:products).map(p=><ProductCard key={p.slug} product={p}/>)}</div></div></main>}
