import { loginAction } from "../actions";
export default async function AdminLogin({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="section"><div className="container" style={{maxWidth:520}}><div className="card card-body"><span className="eyebrow">Administración</span><h1>Iniciar sesión</h1><p className="muted">Usa la contraseña configurada en ADMIN_PASSWORD.</p>{error&&<p style={{color:"var(--error,#ef4444)"}}>Contraseña incorrecta.</p>}<form action={loginAction} className="admin-form"><label>Contraseña<input type="password" name="password" required autoFocus/></label><button className="btn btn-primary" type="submit">Entrar</button></form></div></div></main>}
